vision_encoder:
  model_name: openai/clip-vit-base-patch32
  select_layer: -2
  select_feature: patch
language_model:
  model_name: Qwen/Qwen2.5-0.5B-Instruct
  torch_dtype: bfloat16
connector: {}
data:
  train_data_path: datasets/llava_recap_small/train.json
  image_dir: datasets/llava_recap_small/images
  max_length: 384
  max_train_samples: 2048
training:
  output_dir: ./checkpoints/basic-connector-small
  num_epochs: 1
  per_device_batch_size: 32
  gradient_accumulation_steps: 1
  learning_rate: 0.002
  warmup_ratio: 0.03
  weight_decay: 0.0
  max_grad_norm: 1.0
  bf16: true
  dataloader_num_workers: 8
  logging_steps: 5
  save_steps: 100
  seed: 42
